# Test01 Project

This project is a sample Python application designed to demonstrate the integration of various tools and technologies, including Docker, Jenkins, and Ansible.

## Project Structure

The repository contains the following files:

- `app.py`: The main Python application file.
- `Dockerfile`: Defines the Docker image for the application.
- `jenkinsfile`: Contains the Jenkins pipeline configuration.
- `playbook.yml`: Ansible playbook for configuration management.
- `requirements.txt`: Lists the Python dependencies for the project.
- `inventory`: Ansible inventory file specifying the hosts.
- `templates/`: Directory containing template files used by Ansible.

## Prerequisites

Ensure you have the following installed on your system:

- Python 3.x
- Docker
- Jenkins
- Ansible

## Setup and Installation

1. **Clone the repository**:

   ```bash
   git clone https://github.com/yatara21/test01.git
   cd test01
